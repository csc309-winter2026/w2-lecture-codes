# Lecture 2 Codes

## Inline Scripts

To run inline scripts, you have two options:

1. Install the `Venkat` extension, select the code snippet, and run it via the command palette
2. Use the browser's console tab

## Inline HTML

Open a browser to view the inline JavaScript's effects in the HTML file. Use the browser's inspect panel to explore the DOM and debug.
